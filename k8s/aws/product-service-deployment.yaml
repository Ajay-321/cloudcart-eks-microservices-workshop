apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: cloudcart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      serviceAccountName: cloudcart-dynamodb
      containers:
        - name: product-service
          image: <ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/cloudcart-product-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:

            - name: USE_DYNAMODB
              value: "true"
            - name: AWS_REGION
              value: "us-east-1"

            # NOTE: table name must match scripts/create-dynamodb-tables.sh (plural)
            - name: DYNAMODB_TABLE
              value: "cloudcart-products"

          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
