apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: cloudcart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      serviceAccountName: cloudcart-dynamodb
      containers:
        - name: auth-service
          image: <ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/cloudcart-auth-service:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:

            - name: USE_DYNAMODB
              value: "true"
            - name: AWS_REGION
              value: "us-east-1"

            # NOTE: table name must match scripts/create-dynamodb-tables.sh
            - name: DYNAMODB_TABLE
              value: "cloudcart-users"

            # Demo secret. For a real deployment, put this in a Kubernetes
            # Secret (or AWS Secrets Manager) instead of a plain env value.
            - name: JWT_SECRET
              value: "cloudcart-dev-secret-change-me"

          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
